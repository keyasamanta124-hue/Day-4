#include<stdio.h>
int main() {
    int roll number;
    printf("Enter a roll number:");
    scanf("%d", & roll number);
    if(rool number == 100)
    {
            printf("roll number 100 is present");
    }
    else
    {
        printf("roll number 100 is  not present");
    }
    return 0:
}
                   
